{
  "scripts": {
    "watch": "npx tailwindcss -i ./tailwind.css -o ./public/src/tailwind.css --watch",
    "vercel": "npm install date-fns --save && npx tailwindcss -i ./tailwind.css -o ./public/src/tailwind.css && minify-all-js ./public/src -j -m -M -p"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.24"
  },
  "dependencies": {
    "date-fns": "^2.28.0"
  }
}
